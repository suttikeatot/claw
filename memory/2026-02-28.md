# Session Log: 2026-02-28

## 🎯 สิ่งที่ทำวันนี้

---

## 1. Facebook Login Session Recovery

### ปัญหา:
- รอบก่อนที่ login Facebook ได้ (2-3 สัปดาห์ก่อน) ใช้ session เดิมไม่ได้
- ลองใช้ cookies ที่ส่งมา → ไม่ผ่าน (Facebook reject จาก IP/location ใหม่)
- พยายาม login ใหม่ → ไม่เห็น 2FA

### Root Cause:
**กุ้งใช้คำสั่งผิด:**
```bash
# ❌ ผิด (ที่เคยทำ)
agent-browser open https://facebook.com
agent-browser state load fb-session.json   # ERROR: ไม่ทำงาน!

# ✅ ถูกต้อง
agent-browser open https://facebook.com --state fb-session.json
```

**Key Lesson:** State ต้องโหลดตั้งแต่ตอน `open` ใช้ `--state` flag เท่านั้น

### Solution:
1. ปิด browser เก่า: `agent-browser close`
2. เปิดใหม่พร้อม session: `agent-browser open https://facebook.com --state fb-session.json`
3. เข้าได้ทันที (ไม่ต้อง 2FA ซ้ำ)
4. Save state ใหม่เพื่อ refresh: `agent-browser state save fb-session.json`

### ผลลัพธ์:
- ✅ Login สำเร็จ
- ✅ เข้า Saved posts ได้
- ✅ Session refreshed และใช้งานได้ต่อ

---

## 2. Memory Structure & Organization

### ปัญหาที่พบ:
- ข้อมูลกระจัดกระจาย ไม่มีแผนที่ (FB_LOGIN.txt ซ้ำกับ MEMORY.md)
- ไม่รู้ว่าไฟล์ไหนอยู่ไหน
- ไม่มี logic การค้นหา

### สิ่งที่สร้าง/แก้ไข:

#### A. ลบไฟล์ซ้ำซ้อน
- ❌ ลบ `FB_LOGIN.txt` (ซ้ำกับ MEMORY.md)

#### B. สร้าง WORKSPACE_INDEX.md
ไฟล์แผนที่สำหรับหาไฟล์ต่างๆ ใน workspace:
- Root level files (AGENTS.md, MEMORY.md, etc.)
- memory/ directory
- knowledge/ directory  
- skills/ directory
- projects/ directory

#### C. อัปเดต AGENTS.md
เพิ่มขั้นตอนอ่าน WORKSPACE_INDEX.md ตอนเริ่ม session

---

## 3. Semantic Search & Memory Index

### ความรู้ใหม่:

#### A. How it works (ระดับ mechanism):
- **ไม่ใช่ keyword search** → ใช้ vector similarity
- Text → Embedding (768-dim vector) ผ่าน Gemini API
- เก็บใน SQLite + sqlite-vec extension
- ค้นหา: Query → Vector → Cosine Similarity → Top-k results

#### B. Architecture Layers:
```
L4: Query Interface (user input)
L3: Vector Search Engine (sqlite-vec, cosine similarity)
L2: Embedding Storage (SQLite, chunks + vectors)
L1: Document Processing (chunking, Gemini API)
```

#### C. Indexing Process:
1. อ่าน .md files → แบ่ง chunks (512-1024 tokens, overlap 50%)
2. ส่งผ่าน Gemini API → ได้ vector[768]
3. เก็บใน SQLite (chunks table + vec_chunks virtual table)

#### D. Key Technical Details:
- **Chunking with overlap:** ป้องกันข้อมูลหายตรงขอบ
- **Cosine similarity:** (A·B)/(||A||×||B||) วัดมุมระหว่าง vectors
- **IVF Index:** O(log n) search แทน O(n)
- **768 dimensions:** ขนาด vector จาก Gemini

### สถานะปัจจุบัน:
- ✅ Memory Search: Enabled
- ✅ API Key: อัปเดตเป็น key ใหม่ (ไม่หลุด)
- ✅ Index: 61/61 files · 166 chunks (สำเร็จแล้ว)
- Provider: Gemini (gemini-embedding-001)
- Database: ~/.openclaw/memory/main.sqlite

---

### 🔧 **CRITICAL: Memory Search Provider Fix**

#### ปัญหา:
ใส่ API key ใน `auth-profiles.json` แล้ว แต่ `openclaw memory status` ยังขึ้น:
```
Provider: none (requested: auto)
Embeddings error: No API key found for provider "google"
```

#### Root Cause:
1. **สร้าง profile ผิดชื่อ** - ใช้ `google:manual` แต่ mappings ชี้ไป `google:gemini-api`
2. **ใช้ field ผิด** - `"apiKey"` ไม่รู้จัก ต้องใช้ `"key"`
3. **Mappings ไม่ตรง** - `"google": "google:gemini-api"` แต่ไม่มี profile นั้น

#### Log ที่บอกปัญหา:
```
Skipping memory file sync in FTS-only mode (no embedding provider)
```

#### Solution (ขั้นตอนแก้ไข):

**ไฟล์:** `~/.openclaw/agents/main/agent/auth-profiles.json`

**แก้ไข 3 อย่าง:**

1. **เปลี่ยน field จาก `"apiKey"` เป็น `"key"`:**
```json
"google:manual": {
  "type": "api_key",
  "provider": "google",
  "key": "AIzaSyBiNsbTvwXTLp6bEy9hdszBcVtwOnt5X24"  // ใช้ "key" ไม่ใช่ "apiKey"
}
```

2. **เปลี่ยน mappings ให้ชี้มา profile ที่มี:**
```json
"google": "google:manual"  // แทนที่ "google:gemini-api"
```

3. **Restart gateway:**
```bash
openclaw gateway stop
openclaw gateway start
```

#### ผลลัพธ์:
```
Provider: gemini ✅
Model: gemini-embedding-001 ✅
Indexed: 61/61 files · 166 chunks ✅
```

#### Pattern ที่ถูกต้องจาก providers อื่น:
```json
// kimi-coding (ที่ใช้ได้แล้ว)
"kimi-coding:default": {
  "type": "api_key",
  "provider": "kimi-coding",
  "key": "sk-..."
},
"kimi-coding": "kimi-coding:default"  // mappings ชี้ถูก
```

#### Key Lessons:
- `"apiKey"` ❌ → `"key"` ✅
- Mappings ต้องชี้ไป profile ที่มีอยู่จริง
- ถ้าใช้ `

### ปัญหาที่พบ:
- API Key เก่าโดน block ("reported as leaked")
- ต้องใช้ `export GOOGLE_API_KEY=...` ก่อน index
- Index ช้า (ต้องเรียก API ทุก chunk)

---

## 4. Disaster Recovery & Backup

### Scripts ที่สร้าง/อัปเดต:

#### A. backup-config.sh (มีอยู่แล้ว)
- Backup: openclaw.json, auth-profiles.json, cron/jobs.json
- เก็บที่: ~/.openclaw/backups/
- Auto-cleanup: เก็บ 50 backups ล่าสุด

#### B. restore-config.sh (มีอยู่แล้ว)
- Interactive restore เลือก backup จาก list
- Restore แบบ step-by-step
- มี confirm ก่อน overwrite

#### C. emergency-restore.sh (สร้างใหม่)
สำหรับกรณีระบบพังทั้งหมด:
```bash
# ทำอะไรบ้าง:
1. Stop gateway
2. Backup broken install → ~/.openclaw.broken.YYYYMMDD
3. Clone workspace from git
4. Restore configs from backup
5. Reinstall skills
6. Rebuild memory index
7. Start gateway
```

### Git Backup วันนี้:
**Commit 1:** `c5ece21`
- Add WORKSPACE_INDEX.md
- Update AGENTS.md
- Update MEMORY.md (Facebook Login Guide)
- Update fb-session.json (refreshed)
- Add memory/2026-02-26.md

**Commit 2:** `11a72b7`
- Add scripts/emergency-restore.sh

### สิ่งที่ต้อง Backup (Complete List):
| ส่วน | สำคัญ | Backup ยังไง |
|------|-------|-------------|
| workspace/ (git) | 🔴 สำคัญมาก | git push |
| openclaw.json | 🔴 Config หลัก | backup-config.sh |
| auth-profiles.json | 🔴 API Keys | backup-config.sh |
| cron/jobs.json | 🟡 Cron jobs | backup-config.sh |
| memory/ | 🟡 Semantic index | Rebuild ได้ |
| skills/ | 🟢 Reinstall ได้ | skills-list |

---

## 5. Key Lessons Learned

### A. agent-browser Session Management:
```bash
# ✅ ถูกต้องเสมอ
agent-browser open URL --state file.json

# ❌ ไม่มีทางทำงาน
agent-browser open URL
agent-browser state load file.json
```

### B. Memory Organization:
- อย่าสร้างไฟล์ซ้ำซ้อน (FB_LOGIN.txt vs MEMORY.md)
- ใช้ WORKSPACE_INDEX.md เป็นแผนที่
- อ่าน AGENTS.md ทุกครั้งตอนเริ่ม session

### C. Semantic Search:
- ไม่ใช่ keyword matching → เป็น vector similarity
- ใช้ API แพง (ต้องจ่ายต่อ request)
- Index ช้าแต่ search เร็ว

### D. Backup Strategy:
- Git = สำคัญที่สุด (workspace)
- Configs = ใช้ backup-config.sh
- มี emergency-restore.sh เผื่อฉุกเฉิน

---

## 📝 Files Modified/Created Today:

| ไฟล์ | การกระทำ | หมายเหตุ |
|------|----------|----------|
| `FB_LOGIN.txt` | ❌ ลบ | ซ้ำกับ MEMORY.md |
| `WORKSPACE_INDEX.md` | ✅ สร้างใหม่ | แผนที่ไฟล์ใน workspace |
| `AGENTS.md` | ✏️ แก้ไข | เพิ่มให้อ่าน INDEX |
| `MEMORY.md` | ✏️ แก้ไข | เพิ่ม Facebook Login Guide |
| `scripts/emergency-restore.sh` | ✅ สร้างใหม่ | Disaster recovery |
| `facebook_cookies.txt` | ❌ ลบ | Sensitive data |
| `facebook-login-temp.json` | ❌ ลบ | Temp file |
| `memory/2026-02-28.md` | ✅ สร้างใหม่ | ไฟล์นี้ |

---

## 🎯 Action Items:

1. **ถ้า Facebook session หมดอายุอีก:**
   ```bash
   agent-browser close
   agent-browser open https://facebook.com --state fb-session.json
   ```

2. **ถ้าต้อง index memory:**
   ```bash
   export GOOGLE_API_KEY="AIzaSyBiNsbTvwXTLp6bEy9hdszBcVtwOnt5X24"
   openclaw memory index --force
   ```

3. **ถ้าระบบพัง:**
   ```bash
   export OPENCLAW_WORKSPACE_GIT="git@github.com:suttikeatot/claw.git"
   bash scripts/emergency-restore.sh
   ```

4. **ก่อนทำอะไรเสี่ยง:**
   ```bash
   ~/.openclaw/scripts/backup-config.sh "before-risky-change"
   ```

---

*Session completed: Facebook login ✅ | Memory index explained ✅ | Backup system ✅*
