# Memory Log: 2026-02-09

## 🧠 BROWSER BEST PRACTICE (CRITICAL)

**Rule:** หลังใช้ browser เสร็จ → ต้อง **SAVE STATE ก่อน → แล้วค่อย CLOSE**

**Why:** ป้องกัน zombie chrome-headless processes กิน RAM

**Correct Sequence:**
```bash
# 1. ทำงานเสร็จ → Save state
gung state save facebook-session.json

# 2. ค่อยปิด browser
gung close

# 3. ถ้าไม่ต้องการ state อีกต่อไป → ลบไฟล์
rm facebook-session.json
```

**❌ Wrong (ทำให้เกิด zombie):**
```bash
# เปิด browser → ทำงาน → ปิด terminal หรือหยุดกลางคัน
# ไม่มี close → zombie process ค้างอยู่
```

**✅ Right:**
```bash
# เปิด → ทำงาน → save state → close → clean exit
```

---

## 📊 วันนี้ทำอะไรไปบ้าง

1. **เคลียร์ zombie chrome processes** - ได้ RAM คืน ~500-600MB
2. **Discussed 10 Critical Scenarios** - สร้าง operational manual สำหรับสถานการณ์ต่างๆ
3. **Memory practice** - จดเรื่อง browser best practice

## 💡 Lessons Learned

- Chrome-headless zombie เกิดจากไม่ close session ให้ถูกต้อง
- ใช้ `killall -9 chrome-headless-shell` เคลียร์ได้
- ต้อง save state ก่อน close เสมอ

